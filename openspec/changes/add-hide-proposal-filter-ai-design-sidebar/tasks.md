## 1. 筛选状态与配置扩展

- [x] 1.1 在 DocTreeSidebar 的筛选状态中新增 `hideProposals` 布尔字段，并设置默认值为 `false`。
- [x] 1.2 将“隐藏提案”chip 接入现有筛选 chip 配置与点击切换逻辑，保证与其他 chip 交互一致。
- [x] 1.3 更新筛选状态持久化读写逻辑，确保 `hideProposals` 可保存与恢复，并兼容旧版本缺失字段。

## 2. 文档树过滤逻辑实现

- [x] 2.1 在文档树可见性计算中增加提案类型识别（Proposal/Design/Delta Spec/Tasks），统一抽象为提案节点判定。
- [x] 2.2 当 `hideProposals` 激活时，过滤掉提案目录与提案子文档节点，保留原始需求与 PRD 节点。
- [x] 2.3 保持“先筛选后分组”的顺序，将“隐藏提案”与“仅本需求/过滤已PR/过滤已Lock”按 AND 逻辑叠加。

## 3. 回归验证与边界检查

- [x] 3.1 验证单提案平铺与多提案文件夹两种模式下，“隐藏提案”开关行为均正确。
- [x] 3.2 验证与现有筛选组合时的空视图与分组渲染结果，不出现空分组或异常节点。
- [x] 3.3 在 AI 设计页面手工回归：默认态、激活态、取消态以及刷新后状态恢复均符合规格。
