## 1. 顶部导航概览迁移

- [x] 1.1 移除 AI 设计页中独立概览信息行，避免额外占用一行高度。
- [x] 1.2 将“总需求 / 已通过 / 含文档”三项 badge 放入顶部导航同一行右侧区域。
- [x] 1.3 校准三项统计口径与看板一致（总需求、已通过=approved+submitted、含文档沿用看板规则）。

## 2. 模型切换下拉框接入

- [x] 2.1 在顶部导航新增模型切换下拉框，显示当前生效模型。
- [x] 2.2 接入模型选项来源（Claude/GLM/ARK + 可用的 Custom）并处理 Custom 不可用场景。
- [x] 2.3 实现切换后持久化（复用现有 `ai_model_selected`）并验证后续 AI 请求使用新模型。

## 3. 布局与回归验证

- [x] 3.1 验证导航单行布局下，返回按钮、路径、概览 badge、模型下拉、设置入口不重叠。
- [x] 3.2 验证刷新后模型选择恢复，且 AI 设计页交互（文档树/编辑器/Chatbot）不受影响。
- [x] 3.3 执行构建检查并记录结果（如有既有 warning，注明非本次引入）。

构建记录：在 `pm-ai-app` 执行 `npm run build` 通过；存在既有重复键 warning（`proposal`、`border`），非本次改动引入。
