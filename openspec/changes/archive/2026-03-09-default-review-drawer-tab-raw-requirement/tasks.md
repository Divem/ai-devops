## 1. 抽屉默认 Tab 行为调整

- [x] 1.1 在 `DetailDrawer` 中将默认激活 tab 的初始状态从“评审报告”改为“原始需求”
- [x] 1.2 保持 tab 切换交互不变，确认用户可手动切换到“评审报告”

## 2. 原始需求首屏与空态校验

- [x] 2.1 验证抽屉首次打开时展示原始需求基础信息与正文
- [x] 2.2 验证 `rawRequirement` 为空时，默认 tab 下仍展示 PRD 摘录示例与“示例内容”标识

## 3. 回归与构建验证

- [x] 3.1 回归验证主操作按钮策略（评审/设计）不受默认 tab 调整影响
- [x] 3.2 执行构建验证并记录结果（若有既有 warning，注明非本次引入）
