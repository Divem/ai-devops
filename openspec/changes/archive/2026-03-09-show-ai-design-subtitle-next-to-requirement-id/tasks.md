## 1. 头部信息布局调整

- [x] 1.1 调整 AI 设计页 `DocEditor` 头部上下文区域为“需求编号 + 副标题”同行显示
- [x] 1.2 确保副标题位于需求编号右侧，且不影响文档类型标签区
- [x] 1.3 保持操作按钮区（编辑/提交/生成提案）布局与点击行为不变

## 2. 可读性与溢出策略

- [x] 2.1 为副标题保留单行省略策略，避免长标题挤压头部按钮
- [x] 2.2 在不同文档类型与不同标题长度下验证显示一致性

## 3. 回归与验收

- [x] 3.1 验证 AI 设计页在常见桌面宽度下头部展示符合预期
- [x] 3.2 验证文档切换时需求编号与副标题同步更新
- [x] 3.3 执行构建检查并记录结果

构建记录：在 `pm-ai-app` 执行 `npm run build`，构建成功；存在既有重复键告警（`proposal`、`border`），与本次改动无直接关系。
