## 1. 分组维度与状态建模

- [x] 1.1 在 `DocTreeSidebar` 新增分组维度配置（包含 none/space/subsystem/app/iteration/priority/status/demander/productManager）。
- [x] 1.2 新增 `groupBy` 状态与切换逻辑，并在侧边栏顶部渲染分组维度选择器。
- [x] 1.3 新增 `expandedGroups` 状态，支持组级展开/收起交互。

## 2. 文档树分组渲染实现

- [x] 2.1 基于现有筛选结果 `visibleCards` 计算 `groupedCards`，实现“先筛选后分组”管线。
- [x] 2.2 在分组模式下渲染“分组节点 -> 需求卡片节点 -> 文档节点”层级，并保持现有文档节点行为不变。
- [x] 2.3 对维度缺失值统一映射到“未分配”分组，确保需求卡片不会因空值丢失。
- [x] 2.4 分组模式下仅渲染非空分组，并在组头展示需求数量。

## 3. 新增维度（提出人/受理人）与排序细节

- [x] 3.1 将提出人（需求方）映射到 `demander` 字段并纳入分组维度。
- [x] 3.2 将受理人（产品经理）映射到 `productManager` 字段并纳入分组维度。
- [x] 3.3 为 `priority/status/iteration` 实现稳定排序策略，其它维度按文本排序。

## 4. 回归验证与收尾

- [x] 4.1 验证分组与筛选 chip 叠加场景下的可见性正确性（含“仅本需求/过滤已PR/过滤已Lock”组合）。
- [x] 4.2 验证分组展开收起、文档节点选中、提案文件夹展开与右侧编辑联动不受影响。
- [x] 4.3 补充变更说明并执行构建验证，确认无新增构建错误。
