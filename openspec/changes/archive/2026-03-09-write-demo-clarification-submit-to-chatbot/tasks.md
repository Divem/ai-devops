## 1. 演示弹框提交写入 Chatbot

- [x] 1.1 在演示弹框确认提交成功分支复用 Q/A 摘要构建逻辑
- [x] 1.2 生成 `clarification_summary` 消息并追加到当前需求 chatHistory
- [x] 1.3 为演示路径消息写入来源标识（如 `source: clarification_demo`）
- [x] 1.4 保持校验失败时不写入任何摘要消息

## 2. 渲染与一致性

- [x] 2.1 复用现有 Chatbot 摘要卡片渲染分支，确保演示消息样式一致
- [x] 2.2 验证演示路径与真实评审路径均可显示结构化 Q/A 卡片

## 3. 回归与验收

- [x] 3.1 验证演示弹框单次提交后追加一条消息
- [x] 3.2 验证多次演示提交会连续追加且不覆盖历史
- [x] 3.3 验证切换需求后返回，演示摘要仍可恢复
- [x] 3.4 执行构建检查并记录结果

构建记录：在 `pm-ai-app` 执行 `npm run build` 构建通过；存在历史重复键告警（`proposal`、`border`），与本次演示提交写入逻辑无直接关系。
