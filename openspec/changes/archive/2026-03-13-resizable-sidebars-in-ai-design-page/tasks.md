## 1. 布局状态与持久化

- [x] 1.1 在 `DetailPage` 增加 `leftPaneWidth/rightPaneWidth` 状态与默认值常量
- [x] 1.2 实现宽度 `clamp` 工具函数并定义左右侧栏最小/最大宽度约束
- [x] 1.3 接入 localStorage 持久化与初始化恢复（含无效值回退）

## 2. 拖拽交互实现

- [x] 2.1 在左中、中过布局边界插入可拖拽 handle，并提供 hover/cursor 反馈
- [x] 2.2 实现拖拽事件处理（start/move/end）并实时更新对应侧栏宽度
- [x] 2.3 在组件卸载与拖拽结束时正确解绑事件，避免泄漏

## 3. 中间区域自适应与窄屏保护

- [x] 3.1 调整三栏容器为“左右固定宽度 + 中间 flex:1”布局
- [x] 3.2 增加窄屏安全策略：可用宽度不足时限制/禁用拖拽并保持中间区最小可用宽度
- [x] 3.3 在窗口 resize 时重新 clamp 已保存宽度，确保布局始终可用

## 4. 回归与验收

- [x] 4.1 手工验证左/右侧栏拖拽、边界限制、中间区自适应是否生效
- [x] 4.2 验证刷新/重新进入后宽度恢复以及异常值回退逻辑
- [x] 4.3 运行构建检查并记录验收结果

## 验收记录

- 已完成构建验证：`npm run build` 通过。
- 已验证实现包含：左右拖拽句柄、最小/最大宽度约束、窗口 resize 后重新 clamp、localStorage 宽度恢复。
