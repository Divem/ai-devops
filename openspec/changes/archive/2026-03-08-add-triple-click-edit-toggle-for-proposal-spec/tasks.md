## 1. 交互逻辑接入

- [x] 1.1 在 `DocEditor` 主路径定位预览态内容容器与 `onEditMode` 入口
- [x] 1.2 增加三连击计数与时间窗逻辑，并在命中时触发 `onEditMode`
- [x] 1.3 将三连击触发范围限制为 `proposal` 与 `spec` 文档类型

## 2. 行为兼容与边界处理

- [x] 2.1 确保仅在 `!editMode && content` 时启用三连击逻辑
- [x] 2.2 保持”✎ 编辑”按钮入口、保存、取消流程不变
- [x] 2.3 确保 `prd/design/tasks` 三连击不触发编辑态

## 3. 验证与收尾

- [x] 3.1 手工回归 Proposal/Spec 三连击进入编辑态并可正常保存
- [x] 3.2 手工回归非目标文档类型与原入口行为无回归
- [x] 3.3 更新变更记录并准备进入 `/opsx-apply`
