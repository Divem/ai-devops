## 1. PRD 页入口与触发

- [x] 1.1 在 `DocEditor` 的 `prd` 非编辑态操作区下方新增“功能演示”小字入口
- [x] 1.2 仅在 `docType === 'prd'` 且存在提案生成主按钮时展示该入口
- [x] 1.3 点击“功能演示”时触发需求澄清交互式弹框打开

## 2. 弹框复用与状态管理

- [x] 2.1 在 `DetailPage` 增加独立弹框开关状态，避免与 ProposalReviewDrawer 状态冲突
- [x] 2.2 复用现有需求澄清交互组件，确保 PRD 入口与看板入口交互一致
- [x] 2.3 关闭弹框后保持当前 PRD 文档上下文，不自动触发“生成提案/更新提案”

## 3. 回归与验收

- [x] 3.1 验证“功能演示”入口样式为低干扰小字，且不挤压主按钮
- [x] 3.2 验证在 `生成提案/更新提案` 两种状态下入口均可见并可用
- [x] 3.3 验证需求澄清弹框打开、关闭与再次打开流程稳定
- [x] 3.4 执行构建检查并记录结果

构建记录：在 `pm-ai-app` 执行 `npm run build` 通过；存在历史重复键告警（`proposal`、`border`），与本次改动无直接关系。
