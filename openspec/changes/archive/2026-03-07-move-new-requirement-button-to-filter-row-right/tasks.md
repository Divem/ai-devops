## 1. 布局结构调整

- [x] 1.1 定位看板页筛选区与“新建需求”按钮当前 JSX 结构及样式定义
- [x] 1.2 重构工具行容器，合并为“左侧筛选 + 右侧新建按钮”的同排布局
- [x] 1.3 清理迁移后冗余样式与旧位置占位，确保页面结构简洁

## 2. 响应式与行为一致性

- [x] 2.1 为工具行添加宽度不足时的自适应策略（如 flex-wrap/间距调整）
- [x] 2.2 验证“新建需求”按钮文案、点击、弹窗与提交流程保持不变
- [x] 2.3 验证筛选功能（空间/迭代等）在新布局下交互与结果保持一致

## 3. 多入口回归与验收

- [x] 3.1 检查并同步相关预览入口（SDD/SDD2/Vibe）对应看板实现的布局一致性
- [x] 3.2 手工回归桌面端与窄屏场景，确认无遮挡、重叠、错位
- [x] 3.3 更新变更说明并记录验收结果，准备进入 `/opsx-apply` 实施阶段

## 验收记录

- 实施文件：`pm-ai-app/src/PMPlatform.jsx`
- 结构变更：将“新建需求”下拉按钮从导航栏右上角迁移至 `MultiSelectFilter` 右侧动作区。
- 布局策略：筛选行保持 `flex` + `flex-wrap`，新增右侧动作容器（含清除筛选 + 新建需求）。
- 行为验证：新建需求/导入需求菜单项与原点击逻辑保持一致（`setShowAdd`、`setShowImportDrawer`）。
- 一致性检查：其他预览入口未实现同款筛选行结构，本次无需同步代码迁移。
- 代码检查：执行 `npm run lint`，存在仓库既有 ESLint 报错（与本次改动无关）。
