## 1. 示例数据与解析封装

- [x] 1.1 在前端新增 `PM_AI_Plagform_PRD` 摘录示例常量（基础信息 + 正文）
- [x] 1.2 封装统一解析函数：真实原始需求优先，缺失时回退到 PRD 示例
- [x] 1.3 为示例命中场景输出 `isSample` 标识，供 UI 展示提示文案

## 2. 看板抽屉接入

- [x] 2.1 在 `DetailDrawer` 的“原始需求”Tab 接入统一解析函数
- [x] 2.2 当无真实数据时展示 PRD 摘录示例（基础信息 + 正文）
- [x] 2.3 为示例场景添加“示例内容（来自 PRD 摘录）”标识

## 3. AI 设计页接入

- [x] 3.1 在 AI 设计页“原始需求”文档视图接入同一解析函数
- [x] 3.2 当无真实数据时展示与抽屉一致的示例内容
- [x] 3.3 保持“原始需求”文档只读渲染规则不变

## 4. 回归与记录

- [x] 4.1 验证有真实原始需求时不显示示例兜底
- [x] 4.2 验证无真实原始需求时两个页面均显示相同示例
- [x] 4.3 在本 change 中记录实现与验证结论，便于归档追踪

## 5. 实现记录

- 已新增 `RAW_REQUIREMENT_SAMPLE_FROM_PRD` 示例常量，示例内容来自 `PRD_EXAMPLE_SNIPPET` 摘录。
- 已新增 `getDisplayRawRequirement(card)` 统一解析函数，输出 `content/baseInfo/isSample`。
- 已在看板抽屉与 AI 设计页原始需求只读视图接入统一解析逻辑。
- 当 `rawRequirement` 为空时，两个页面均展示 PRD 摘录示例，并显示“示例内容（来自 PRD 摘录）”。
- 保持真实数据优先与原有编辑/同步行为不变，不会将示例内容写回卡片。
- 已执行 `npm run build`，构建通过（存在仓库历史 duplicate key warning，非本次变更引入）。
