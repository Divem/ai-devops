## 1. 项目配置区块布局调整

- [x] 1.1 梳理 `ProjectSettingsPage` 中项目配置区块的现有结构，确定左侧选择区与右侧摘要区的容器拆分方案
- [x] 1.2 将关联空间、关联子系统、关联项目（应用）三组控件重组到左侧选择区，并保留现有级联禁用与空状态逻辑
- [x] 1.3 在右侧新增结构化摘要区域，分别展示已选空间、子系统、应用及未选择时的占位提示

## 2. 交互与响应式完善

- [x] 2.1 补充左右结构下的操作说明文案，使用户明确“先选空间，再选子系统，再选应用”的顺序
- [x] 2.2 为项目配置区块添加窄屏退化规则，在宽度不足时切换为上下结构并保持内容可读
- [x] 2.3 检查已选摘要与左侧选择状态的同步更新，确保取消上层选择时下层结果同步清理

## 3. 验证与收尾

- [ ] 3.1 手动验证空间、子系统、应用多选、保存、刷新恢复与空状态展示是否正常
- [x] 3.2 运行项目可用的校验命令（如 `npm run lint`），确认本次布局调整未引入明显问题
