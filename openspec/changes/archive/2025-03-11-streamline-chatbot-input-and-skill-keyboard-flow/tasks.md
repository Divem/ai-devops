## 1. 输入区简化改造

- [x] 1.1 移除 `ChatbotPanel` 中 Markdown 预览输入模式相关状态与 UI（preview/edit 切换）
- [x] 1.2 将输入区固定为直接编辑 textarea，并保留 Enter/Shift+Enter 基础发送规则
- [x] 1.3 更新输入区提示文案，明确按键语义

## 2. 技能面板键盘行为调整

- [x] 2.1 在 slash 面板打开态下实现 Tab 行为：关闭/退出面板并将焦点返回输入框
- [x] 2.2 在 slash 面板打开态下实现 Enter 行为：执行当前高亮技能并直接发送消息
- [x] 2.3 保持 Esc 关闭、上下键切换和鼠标选择行为不回归

## 3. 发送链路与兼容性

- [x] 3.1 校正技能执行后的输入清理与状态重置，避免重复触发
- [x] 3.2 确保普通聊天与技能聊天都写入会话历史并可重试
- [x] 3.3 确保 fallback、thinking trace 与消息渲染兼容

## 4. 验收与回归

- [x] 4.1 手工验证无预览模式下普通聊天发送体验
- [x] 4.2 手工验证 slash 面板 Tab/Enter 行为符合预期（Tab 不发送、Enter 直接发送）
- [x] 4.3 执行 `npm run build` 并记录验收结果

## 验收记录

- 已完成构建验证：在 `pm-ai-app` 执行 `npm run build` 通过。
- 已确认 Chatbot 输入区移除预览模式，保留单一 textarea 输入。
- 已确认 slash 面板键盘行为：Tab 仅返回输入且不发送，Enter 直接执行并发送高亮技能。
