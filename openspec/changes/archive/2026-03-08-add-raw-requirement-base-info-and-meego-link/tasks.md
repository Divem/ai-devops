## 1. 字段映射与数据兼容

- [x] 1.1 在 `pm-ai-app/src/PMPlatform.jsx` 为 `DetailDrawer` 增加原始需求基础信息映射函数（需求方、产品经理、提报时间、所属模块、meegoUrl）
- [x] 1.2 映射逻辑优先读取结构化字段（如 `rawRequirementMeta`），并兼容历史字段命名
- [x] 1.3 对缺失字段统一返回空值，对非法链接按不可用处理

## 2. 原始需求 Tab UI 增强

- [x] 2.1 在「原始需求」Tab 顶部新增基础信息展示区，固定四个字段标签
- [x] 2.2 为基础信息缺失值增加统一回退展示 `-`
- [x] 2.3 新增 Meego 链接展示区：有链接时渲染可点击入口、无链接时显示"未关联 Meego 链接"
- [x] 2.4 保持原始需求正文展示与编辑交互不变（`white-space: pre-wrap`、编辑/保存/取消）

## 3. 交互与可用性校验

- [x] 3.1 校验 Meego 链接点击行为为新窗口打开且不影响抽屉当前状态
- [x] 3.2 验证字段完整场景：基础信息与链接显示正确
- [x] 3.3 验证字段缺失场景：基础信息回退值和链接空态文案正确
- [x] 3.4 验证非法链接场景：不渲染可点击链接并显示空态

## 4. 回归与文档同步

- [x] 4.1 回归验证原始需求编辑保存流程
- [x] 4.2 回归验证"同步到 Meego"按钮在已配置/未配置场景下行为不变
- [x] 4.3 在 change 内记录实现结果与必要说明，确保后续归档可追踪

## 5. 实现记录

- 已在 `DetailDrawer` 增加 `rawRequirementMeta` 优先的基础信息映射，并兼容历史字段命名。
- 已新增基础信息区（需求方/产品经理/提报时间/所属模块）及 `-` 回退展示策略。
- 已新增 Meego 链接区，合法 URL 渲染为新窗口入口，不合法或缺失时显示"未关联 Meego 链接"。
- 已保持原有原始需求正文编辑与同步到 Meego 行为不变。
- 已执行 `npm run build` 验证可构建通过（存在仓库历史 duplicate key 警告，非本变更引入）。
