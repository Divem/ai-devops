## 1. 右侧面板结构扩展

- [x] 1.1 在 AI 设计页面右侧面板中新增“版本记录”区域，并放置在 Chatbot/参考资料之后。
- [x] 1.2 调整右侧面板导航与布局，确保新增区域在桌面与常见分辨率下可正常访问。
- [x] 1.3 为无版本数据场景提供空状态展示（引导用户提交首个版本）。

## 2. 版本日志数据模型与渲染

- [x] 2.1 在 docs 数据结构中增加版本日志字段（如 versionHistory）并实现向后兼容默认值。
- [x] 2.2 定义单条版本记录信息（版本标识、时间、提交说明、关联文档、提交链接/sha）。
- [x] 2.3 在版本记录区域按时间顺序渲染版本列表并突出最新版本。

## 3. 提交为新版本工作流

- [x] 3.1 在版本记录区域增加“提交为新版本”按钮与提交说明输入（或默认说明）交互。
- [x] 3.2 复用现有 GitHub 提交流程，提交成功后新增版本记录并回写到当前卡片 docs。
- [x] 3.3 提交失败时阻止写入版本记录并显示错误提示，确保日志与真实提交状态一致。

## 4. 验证与回归

- [x] 4.1 验证提交成功路径：GitHub 生成新提交后，版本记录即时新增且信息正确。
- [x] 4.2 验证提交失败路径：版本记录不新增，用户可见失败原因并可重试。
- [x] 4.3 执行相关 lint/build 检查并记录后续优化项。

## Follow-up Notes

- `npm run build` 通过。
- `npm run lint` 失败，主要为仓库中已存在的历史问题（如 `AIClient` 未定义、重复 key、空 block、hooks 规则等），本次变更未新增阻断构建的问题。
