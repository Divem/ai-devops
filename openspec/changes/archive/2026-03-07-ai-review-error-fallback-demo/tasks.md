## 1. 评审失败兜底数据与状态

- [x] 1.1 在 `PMPlatform.jsx` 新增 AI 评审示例结果常量（结构与 `aiResult` 完全一致）
- [x] 1.2 新增评审失败兜底入口状态（按卡片维度记录是否可使用示例结果）
- [x] 1.3 确保 AI 评审成功后清理该兜底状态

## 2. AI 评审失败分支改造

- [x] 2.1 在 `handleAIReview` 的 catch 分支中保留现有错误提示逻辑
- [x] 2.2 在 catch 分支中标记当前卡片可使用示例评审结果
- [x] 2.3 失败后保持列状态可继续操作，不阻断详情抽屉后续流程

## 3. 示例评审结果注入动作

- [x] 3.1 新增“使用示例评审结果（用于演示）”按钮点击处理函数
- [x] 3.2 点击后将示例 `aiResult` 写入当前卡片并更新列为 `confirm`
- [x] 3.3 写入后自动隐藏兜底按钮，避免重复覆盖

## 4. 评审报告展示标识

- [x] 4.1 在评审报告头部增加“示例数据（非 AI 实时生成）”标签
- [x] 4.2 仅对兜底写入的评审结果展示该标签
- [x] 4.3 真实 AI 评审成功结果不展示该标签

## 5. 手工验证

- [x] 5.1 验证 AI 评审成功路径不受影响（无兜底按钮）
- [x] 5.2 验证 AI 评审失败后出现兜底按钮，点击可生成示例评审结果
- [x] 5.3 验证兜底后可继续执行“人工确认/通过”全流程
