## 1. 技能与命令注册表

- [x] 1.1 在 `PMPlatform.jsx` 增加 Chatbot 工具注册表（skills/commands 统一结构）
- [x] 1.2 内置默认产品经理技能：需求评审、风险检查、相关需求、总结功能点、完备性检查
- [x] 1.3 增加注册表读取逻辑，支持内置默认与现有 Skill 配置兼容

## 2. 输入区 slash 面板交互

- [x] 2.1 在 Chatbot 输入区实现 `/` 触发的面板显隐与检索过滤
- [x] 2.2 实现面板键盘导航（上下选择、Enter 确认、Esc 关闭）
- [x] 2.3 实现鼠标点击选择与高亮态样式（对齐参考交互）

## 3. 技能/命令执行链路

- [x] 3.1 选择条目后生成结构化执行请求并写入会话消息
- [x] 3.2 复用现有发送链路执行技能请求，补充 `source/skillId/commandId` 元信息
- [x] 3.3 执行失败时接入现有 fallback 与重试能力，保证流程可继续

## 4. 结果渲染与可读性

- [x] 4.1 为技能结果新增结构化渲染样式（评分、风险、建议、缺失项等）
- [x] 4.2 保持旧消息类型兼容，未知类型回退纯文本渲染
- [x] 4.3 校验 Markdown 预览模式与技能执行模式的输入行为不冲突

## 5. 回归与验收

- [x] 5.1 手工验证默认技能调用场景（5 个内置技能逐一验证）
- [x] 5.2 手工验证普通聊天、模型切换、历史会话展示不回归
- [x] 5.3 执行 `npm run build` 并记录验收结果

## 验收记录

- 已完成构建验证：在 `pm-ai-app` 执行 `npm run build` 通过。
- 构建输出包含既有 duplicate key warning（`proposal`、`border`），为仓库历史问题，非本次变更引入。
- 已实现 slash 技能面板、键盘/鼠标选择、结构化消息与重试链路复用。
