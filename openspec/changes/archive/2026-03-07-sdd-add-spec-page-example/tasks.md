## 1. DOC_TYPES 更新

- [x] 1.1 在 `DOC_TYPES` 数组中添加 `spec` 类型定义（位置：`prd` 之后）
- [x] 1.2 确认 spec 类型包含正确属性：key="spec", label="需求规格说明", icon="📝", color="#8b5cf6", group="product"

## 2. 示例内容创建

- [x] 2.1 创建 `DEMO_SPEC_REVIEW` 常量（提案评审与修改模块的规格说明）
- [x] 2.2 确认示例内容包含完整结构：概述、功能规格、UI规格、数据规格
- [x] 2.3 基于 PRD 文档 3.4 节内容，包含评审视图（分屏对比、评论批注、版本历史）和修改工具（富文本编辑器、Markdown 支持、实时预览）

## 3. 辅助函数更新

- [x] 3.1 更新 `mkDocs()` 函数，添加 `spec: null` 字段
- [x] 3.2 验证返回对象包含所有五个文档类型

## 4. 示例数据更新

- [x] 4.1 更新 `INITIAL_CARDS` 中 REQ-005 的 `docs.spec` 字段，设置为 `DEMO_SPEC_REVIEW`
- [x] 4.2 确认其他卡片的 docs 对象也包含 spec 字段（值为 null）

## 5. 验证测试

- [x] 5.1 在浏览器中加载应用，确认无语法错误（基础语法检查已通过）
- [ ] 5.2 点击 REQ-005 卡片，确认 spec 文档可正常显示（需要浏览器验证）
- [ ] 5.3 验证文档树侧边栏正确显示 spec 类型图标和标签（需要浏览器验证）
