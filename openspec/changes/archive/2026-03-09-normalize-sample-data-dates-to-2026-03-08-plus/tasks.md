## 1. 示例需求日期梳理

- [x] 1.1 盘点看板示例数据中所有 `date` 字段并标记早于 `2026-03-08` 的项
- [x] 1.2 确认日期调整后仍保持 `YYYY-MM-DD` 格式一致性

## 2. 日期基线更新

- [x] 2.1 将示例需求 `date` 统一更新为 `2026-03-08` 及之后
- [x] 2.2 保持示例需求数量、主题、状态列分布和维度字段不变，仅更新日期

## 3. 回归验证

- [x] 3.1 验证看板卡片日期展示、筛选、详情抽屉打开行为正常
- [x] 3.2 验证数据概览相关统计未因日期格式或范围变化报错

## 4. 构建与收尾

- [x] 4.1 执行构建验证并记录结果（若有既有 warning，注明非本次引入）
- [x] 4.2 更新变更说明，确认日期基线要求已覆盖后续维护场景
