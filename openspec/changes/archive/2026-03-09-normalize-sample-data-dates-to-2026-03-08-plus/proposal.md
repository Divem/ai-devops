## Why

当前看板示例数据中存在较早日期，和当前演示时间线不一致，影响需求流转与趋势解读的可信度。需要统一示例需求日期到 `2026-03-08` 及之后，保证演示口径一致。

## What Changes

- 将看板示例需求卡片中的 `date` 字段统一调整为 `2026-03-08` 及之后的日期。
- 保持示例需求总量、主题、状态分布和维度字段不变，仅更新日期范围。
- 确保日期格式继续符合 `YYYY-MM-DD`，避免影响现有统计与展示逻辑。
- 补充日期范围验收规则，确保后续新增示例数据也遵循该时间基线。

## Capabilities

### New Capabilities
- （无）

### Modified Capabilities
- `kanban-prd-seeded-sample-requirements`: 为看板示例需求增加日期基线约束，要求示例日期为 `2026-03-08` 及之后。

## Impact

- 受影响代码：`pm-ai-app/src/PMPlatform.jsx`（示例卡片 `date` 字段）。
- 受影响范围：仅示例数据内容，不涉及 API、状态机或数据结构变更。
- 风险：低，主要需回归验证看板展示和统计口径无异常。

## 维护约束

- 后续新增或替换内置示例需求时，`date` 字段应保持 `2026-03-08` 及之后，避免演示时间线回退。
