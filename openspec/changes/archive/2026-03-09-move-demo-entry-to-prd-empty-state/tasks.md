## 1. 入口迁移与渲染条件调整

- [x] 1.1 从 PRD 非空头部操作区移除“功能演示”入口
- [x] 1.2 在 PRD 空状态区域中，将“功能演示”入口放到“生成提案（请先生成 PRD）”按钮下方
- [x] 1.3 确保“功能演示”入口仅在 `docType === 'prd'` 且内容为空时显示

## 2. 弹框触发与行为一致性

- [x] 2.1 复用现有需求澄清交互式弹框回调，保证入口迁移后仍可正常打开
- [x] 2.2 保持弹框关闭后停留当前 PRD 文档上下文，不自动触发提案生成
- [x] 2.3 验证看板入口与 PRD 空状态入口触发的弹框内容和校验行为一致

## 3. 回归与验收

- [x] 3.1 验证 PRD 空状态下入口样式为低干扰小字且层级低于主按钮
- [x] 3.2 验证 PRD 非空状态不再展示该入口，头部操作区布局正常
- [x] 3.3 执行构建检查并记录结果

构建记录：在 `pm-ai-app` 执行 `npm run build` 成功；存在既有重复键告警（`proposal`、`border`），与本次入口迁移无直接关系。
