## 1. 配置结构升级与兼容

- [x] 1.1 将 `projectConfig.project` 从单值字段升级为数组字段（`spaceIds`、`subsystemIds`、`appIds`）
- [x] 1.2 在 `loadProjectConfig` 中实现旧数据兼容迁移（单值自动转单元素数组）
- [x] 1.3 更新项目配置保存逻辑，确保多选结果稳定写回并可刷新恢复

## 2. 多选交互与联动逻辑

- [x] 2.1 将空间、子系统、应用控件改为多选交互并展示已选摘要
- [x] 2.2 实现层级联动过滤（空间并集决定子系统候选，子系统并集决定应用候选）
- [x] 2.3 实现上级变化后的下级失效项自动清理

## 3. 页面行为与回归验证

- [x] 3.1 确认默认进入项目关联配置板块并与左侧导航联动一致
- [x] 3.2 手工验证多选保存、刷新回填、失效项清理等关键场景
- [x] 3.3 修复验证中发现的问题并确保构建通过
