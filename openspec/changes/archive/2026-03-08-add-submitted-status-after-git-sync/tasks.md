## 1. 看板状态列扩展

- [x] 1.1 在看板列配置中新增 `submitted` 状态，标题为“已提交”
- [x] 1.2 为“已提交”列配置灰色主题样式，视觉参考“待评审”
- [x] 1.3 校验看板按列分组渲染与计数逻辑兼容 `submitted`

## 2. Git 提交成功后的状态流转

- [x] 2.1 定位“提交到 Git”成功回调中的 `updateCard` 写入逻辑
- [x] 2.2 在提交成功时追加卡片状态更新：`col: "submitted"`
- [x] 2.3 确保 `gitStatus` 持久化与 `submitted` 状态更新在同一成功分支生效

## 3. 兼容性与回归验证

- [x] 3.1 验证历史卡片（无 `submitted`）仍在原有列正常显示
- [x] 3.2 验证提交成功后卡片自动进入“已提交”列
- [x] 3.3 验证提交失败场景不会错误流转到 `submitted`

## 4. 文档与收尾

- [x] 4.1 记录实现结果与验证结论到本 change 的 tasks 文档

## 5. 实现记录

- 已在 `COLUMNS` 新增 `submitted` 列（文案“已提交”），颜色使用与“待评审”一致的灰色风格。
- 已在 Git 提交成功回调中，将 `gitStatus` 持久化与 `col: "submitted"` 状态流转放在同一成功分支。
- 已扩展 `handleUpdateDocs` 以支持额外 patch，确保提交成功时可同步更新卡片状态。
- 已更新看板过滤与统计逻辑，确保 `submitted` 被视为完成态并与旧卡片兼容。
- 已执行 `npm run build` 验证构建通过（存在仓库历史 duplicate key warning，非本次改动引入）。
