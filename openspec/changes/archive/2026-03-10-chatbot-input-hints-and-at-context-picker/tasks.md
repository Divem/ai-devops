## 1. 输入提示文案与输入态联动

- [x] 1.1 在 chatbot 输入框中新增并接入提示文案 `直接提问，@添加上下文，/使用命令和技能`。
- [x] 1.2 确保输入框为空时显示提示文案，有输入内容时自动隐藏。

## 2. @ 上下文候选面板

- [x] 2.1 实现输入 `@` 进入上下文选择态并打开候选面板的触发逻辑。
- [x] 2.2 在候选面板中按分组展示“相关需求”和“相关文档”数据，并提供空状态反馈。
- [x] 2.3 实现候选项高亮/切换与面板开关同步，避免输入变化导致状态残留。

## 3. Enter 回填与发送行为优先级

- [x] 3.1 实现候选面板激活时 Enter 优先确认候选并回填输入框。
- [x] 3.2 实现回填后光标定位在插入内容后，支持继续编辑和连续插入。
- [x] 3.3 确保候选面板关闭时 Enter 保持原有发送语义，不引入误发送回归。

## 4. 回归验证

- [x] 4.1 验证提示文案、`@` 触发、候选分组展示与空状态在常见输入路径下行为正确。
- [x] 4.2 验证键盘交互（上下选择、Enter 确认、关闭后 Enter 发送）符合预期。
- [x] 4.3 在 `pm-ai-app` 执行构建或页面回归检查，确认变更未影响现有 chatbot 输入与发送流程。
