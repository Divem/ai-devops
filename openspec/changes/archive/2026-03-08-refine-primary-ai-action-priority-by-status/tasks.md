## 1. 状态判定与动作模型

- [x] 1.1 在 `DetailDrawer` 中复用/确认“已评审”状态判定（基于 `card.aiResult`）
- [x] 1.2 抽象 AI 动作配置（评审、设计）并按状态生成主次顺序
- [x] 1.3 明确主按钮与次按钮的样式映射规则（主：实色高亮；次：描边弱化）

## 2. 底部操作区渲染调整

- [x] 2.1 修改底部操作区渲染逻辑：始终同时展示“AI 智能评审”和“AI 辅助设计”
- [x] 2.2 未评审态下将“AI 智能评审”置为主按钮并保持“AI 辅助设计”可点击
- [x] 2.3 已评审态下将“AI 辅助设计”置为主按钮并保持“AI 智能评审”可点击

## 3. 交互一致性与回归

- [x] 3.1 验证主次按钮切换后点击行为正确映射到 `onAIReview` 与 `onAIDesign`
- [x] 3.2 验证 `reviewing` 状态下评审按钮 loading/禁用体验不回归
- [x] 3.3 验证通过/拒绝按钮行为与布局不受 AI 双按钮改动影响

## 4. 验证与记录

- [x] 4.1 手工验证两类场景：未评审（评审主/设计次）与已评审（设计主/评审次）
- [x] 4.2 在本 change 中记录实现结果，确保归档前可追踪

## 5. 实现记录

- 已在 `DetailDrawer` 保留 `hasReviewed = Boolean(card.aiResult)` 作为主次切换判定。
- 已新增 `aiActions` 动作模型，未评审时按“评审主/设计次”，已评审时按“设计主/评审次”排序。
- 已将底部操作区改为双按钮常驻渲染，次按钮保持可点击，不再隐藏。
- 已保留 `reviewing` 状态下评审按钮 loading/禁用语义（次按钮场景同样生效）。
- 已执行 `npm run build`，构建通过（存在仓库既有 duplicate key warning，非本变更引入）。
