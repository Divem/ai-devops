## 1. 概览指标数据计算

- [x] 1.1 在 AI 辅助设计页新增概览统计计算逻辑，基于 `cards` 计算“总需求”。
- [x] 1.2 增加“已通过”统计（`col === 'approved'`）并与看板口径对齐。
- [x] 1.3 增加“含文档”统计（原始需求/PRD/任一提案文档）并统一边界判断。

## 2. 页面展示与布局集成

- [x] 2.1 在 AI 辅助设计页顶栏下、三栏主体上方增加概览指标区。
- [x] 2.2 渲染“总需求 / 已通过 / 含文档”三张指标卡，文案与看板概览保持一致。
- [x] 2.3 确保概览区接入后不影响文档树、编辑器、Chatbot 现有布局与交互。

## 3. 空态与回归验证

- [x] 3.1 验证 `cards` 为空或不可用时三项指标回退为 `0` 且页面不报错。
- [x] 3.2 验证看板与 AI 辅助设计页在同一数据下三项指标一致。
- [x] 3.3 执行构建检查并记录结果（若存在既有 warning，注明非本次引入）。

构建记录：在 `pm-ai-app` 执行 `npm run build` 通过；存在既有重复键 warning（`proposal`、`border`），非本次改动引入。
